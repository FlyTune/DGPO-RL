# 极简 CI：仅做通过检查，保证 push 有绿色状态
name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Pass
        run: echo "OK"
